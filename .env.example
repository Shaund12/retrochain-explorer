## REST API base (defaults to app's reverse proxy path)
VITE_REST_API_URL=/api
# VITE_REST_API_URL=https://api.retrochain.ddns.net

## Tendermint RPC HTTP base (Keplr + fallback HTTP client)
VITE_RPC_URL=/rpc
# VITE_RPC_URL=https://rpc.retrochain.ddns.net

## Dev-only (Vite) proxy upstream targets
# VITE_RPC_PROXY_TARGET=http://localhost:26657

## Tendermint RPC WebSocket base (used for tx signing + subscriptions)
VITE_RPC_WS_URL=ws://localhost:26657/websocket
# VITE_RPC_WS_URL=wss://rpc.retrochain.ddns.net/websocket

# Optional dedicated endpoints per network for toggle
VITE_REST_API_URL_TESTNET=/api
VITE_REST_API_URL_MAINNET=/api
VITE_RPC_URL_TESTNET=/rpc
VITE_RPC_URL_MAINNET=/rpc
VITE_RPC_WS_URL_TESTNET=ws://localhost:26657/websocket
VITE_RPC_WS_URL_MAINNET=ws://localhost:26657/websocket

# Launcher (pump.fun-style) UI gate
VITE_ENABLE_LAUNCHER=false

## Maintenance Mode
VITE_MAINTENANCE_MODE=false
VITE_MAINTENANCE_MESSAGE=?? System Upgrade in Progress
VITE_MAINTENANCE_DETAILS=RetroChain is currently undergoing scheduled maintenance. We'll be back online shortly!
VITE_MAINTENANCE_ETA=Expected completion: ~30 minutes
VITE_MAINTENANCE_FEATURES=🔥 Burn module rollout,BTC staking launch,New staking UX
VITE_MAINTENANCE_BLOCK=
VITE_ENABLE_DEX=false

## BattlePoints (optional)
# BattlePoints contract address
# VITE_BATTLEPOINTS_CONTRACT=
# CW20 "Battle Points" token contract address
# VITE_BATTLEPOINTS_CW20=
# CW721 BattlePoints NFT collection contract address
# VITE_BATTLEPOINTS_CW721=

## BattlePoints claims UI gate (Arcade battles)
# Set to "true" to disable on-chain claiming and show a "Coming soon" message.
VITE_BATTLEPOINTS_CLAIMS_DISABLED=true
# Optional override text shown in the Arcade Battle Quests card when disabled.
VITE_BATTLEPOINTS_CLAIMS_DISABLED_MESSAGE=On-chain BattlePoints claiming is temporarily disabled (coming soon).

## Arcade Game Visibility Controls
# Set to "false" to hide individual games from the arcade dashboard and home alerts
VITE_ARCADE_GAME_RETROVADERS_ENABLED=true
VITE_ARCADE_GAME_RETRONOID_ENABLED=true
VITE_ARCADE_GAME_RETROMAN_ENABLED=true

## Cosmos Hub IBC Channels
# Channel ID on RetroChain that points to Cosmos Hub
VITE_IBC_CHANNEL_RETRO_COSMOS=channel-0
# Channel ID on Cosmos Hub that points back to RetroChain (set to actual value before production)
VITE_IBC_CHANNEL_COSMOS_RETRO=channel-1638
# Override Cosmos Hub chain id if needed
VITE_COSMOS_CHAIN_ID=cosmoshub-4
# RPC endpoint Keplr should use when signing Cosmos Hub transactions (needed for Cosmos → Retro transfers)
VITE_COSMOS_RPC_URL=https://rpc.cosmoshub.strange.love:443

## WBTC IBC Denoms
# Hash denom for WBTC once it is on RetroChain (ibc/<hash>)
VITE_IBC_DENOM_WBTC_ON_RETRO=ibc/CF57A83CED6CEC7D706631B5DC53ABC21B7EDA7DF7490732B4361E6D5DD19C73
# Base denom for WBTC on Cosmos Hub (native, defaults to cwbtc)
VITE_DENOM_WBTC_ON_COSMOS=ibc/99B00614DDBE6189AA03B77066FF8EB3F93680BD790C43CF56096B7F23542015

